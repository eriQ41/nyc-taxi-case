# Local copy of the landing data (downloaded locally, uploaded to the UC Volume).
# Never commit the raw parquet — it's large and reproducible from the TLC source.
/data/
*.parquet

# Databricks Asset Bundle local state (per-user, regenerated on deploy; contains
# the deployer's workspace path/email). The CLI also auto-ignores this folder.
.databricks/

# Case prompt (iFood's material) — kept locally, not published in the repo
Case_iFood_-_Data_Architecture.pdf

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
